// REGLAS DE FIREBASE STORAGE CORREGIDAS
// Versión que permite funcionalidad básica

rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // Permitir acceso a usuarios autenticados
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}