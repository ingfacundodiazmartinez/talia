// REGLAS TEMPORALES SIMPLES PARA DESARROLLO
// ⚠️ SOLO PARA TESTING - NO USAR EN PRODUCCIÓN

rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}