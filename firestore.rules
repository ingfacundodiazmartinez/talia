rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ REGLAS TEMPORALES PARA TESTING - MUY INSEGURAS
    // ⚠️ RESTAURAR REGLAS SEGURAS DESPUÉS DE PROBAR

    match /{document=**} {
      allow read, write: if true;
    }
  }
}
