// REGLAS TEMPORALES PARA DESARROLLO - ⚠️ NO USAR EN PRODUCCIÓN

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir lectura y escritura a usuarios autenticados (TEMPORAL)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

// Después de confirmar que funciona, cambia a las reglas de seguridad apropiadas