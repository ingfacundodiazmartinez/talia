{
  "name": "talia-app",
  "version": "1.0.0",
  "description": "Talia - Chat app with parental controls",
  "scripts": {
    "test": "jest --testTimeout=10000",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "jest": "^29.7.0"
  }
}
